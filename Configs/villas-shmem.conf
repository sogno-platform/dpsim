nodes = {
	shmem = {
		type = "shmem",
		out_name = "/villas-dpsim",
		in_name = "/dpsim-villas",
		polling = false
	},
	web = {
		type = "websocket"
	}
}

paths = (
	{
		in = "shmem",
		out = "web",
		reverse = true
	}
)
